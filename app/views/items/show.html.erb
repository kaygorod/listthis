<% if @item.title? %>
  <% provide(:title, @item.title) %>
<% else %>
  <% provide(:title, @list.title) %>
<% end %>
<% provide(:image, @item.image) %>
<% provide(:desc, @item.description) %>

<script> location.href = "<%= list_path(@list) %>"; </script>
