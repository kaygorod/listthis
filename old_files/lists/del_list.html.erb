<li>
  <%= link_to list.title, list_path(list)%> |
  <%= list.items.count %> Пунктов |
  Опубликован <%= time_ago_in_words(list.created_at) %> назад. |
  Просмотры: <%= list.views %> |
  Комментарии: <%= list.comments.count %> |
  <% if can? :update, @list %>
  <%= link_to 'Редактировать', edit_list_path(list) %>
  <% end %>
</li>
